# This task is equivalent to running the following command
# in files/centos7:
#
# docker build -t test:latest .
#
---
- name: Build base image
  docker_image: name=base tag=latest path="{{ role_path }}/files/centos7/"
